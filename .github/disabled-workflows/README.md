# Inactive upstream workflows

These helpers require pull-request, label, or workflow-run event payloads.
The fork starts CI manually, so these upstream definitions are retained here
without registering unusable manual actions.
